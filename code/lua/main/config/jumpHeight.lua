-- Basic parameters for the jpower function in the cmd, may be a bit buggy for now.
while true do
  game.Workspace.LocalPlayer.Humanoid - 
    JumpHeight = %typeSet
  when %typeSet = refer(reverb/code/cmd/main/config/jumpHeight) and find "typeSet"
    loop()
    end
  end
end
